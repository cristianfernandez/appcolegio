{% extends 'TemasTemasBundle::admin.html.twig' %}

{% block article %}

    <h1>Nivel</h1>

    <table class="table table-striped table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <th>Detallenivel</th>
            </tr>
            <tr>
                <td>{{ entity.id }}</td>
                <td>{{ entity.detalleNivel }}</td>
            </tr>
        </tbody>
    </table>

<ul class="record_actions">
    <li>
        <a href="{{ path('nivel_edit', { 'id': entity.id }) }}" class="btn btn-small btn-success"><i class="icon-edit"> </i></a>
        {{ form(delete_form) }}
    </li>
    <li>
        <a href="{{ path('nivel') }}"><i class="icon-arrow-left"> Volver a la lista</i></a>
    </li>
</ul>
{% endblock %}    