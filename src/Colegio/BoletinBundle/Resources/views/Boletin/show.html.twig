{% extends 'TemasTemasBundle::admin.html.twig' %}

{% block article %}

    <h1>Boletin</h1>

    <table class="table table-striped table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <th>Calificacionfinal</th>
                <th>Fechainicio</th>                
                <th>Fechafin</th>
                <th>Activo</th>
            </tr>
            <tr>
                <td>{{ entity.id }}</td>
                <td>{{ entity.calificacionFinal }}</td>
                <td>{{ entity.fechaInicio|date('Y-m-d H:i:s') }}</td>
                <td>{{ entity.fechaFin|date('Y-m-d H:i:s') }}</td>
                <td>{{ entity.activo }}</td>
            </tr>
        </tbody>
    </table>

<ul class="record_actions">
    <li>
        <a href="{{ path('boletin_edit', { 'id': entity.id }) }}" class="btn btn-small btn-success"><i class="icon-edit"> </i></a>
        {{ form(delete_form) }}
    </li>
    <li>
        <a href="{{ path('boletin') }}"><i class="icon-arrow-left"> Volver a la lista</i></a>
    </li>
</ul>
{% endblock %}