{% extends 'TemasTemasBundle::admin.html.twig' %}

{% block article %}

    <h1>Lista Estudiante Boletin</h1>

    <table class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>Id</th>
                <th>Id Estudiante</th>
                <th>Id Boletin</th>
                <th>Id Periodo</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('estudianteboletin_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.idEstudiante }}</td>
                <td>{{ entity.idBoletin }}</td>
                <td>{{ entity.idPeriodo }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('estudianteboletin_show', { 'id': entity.id }) }}" ><i class="icon-list"> </i></a>
                    </li>
                    <li>
                        <a href="{{ path('estudianteboletin_edit', { 'id': entity.id }) }}"><i class="icon-edit"> </i></a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('estudianteboletin_new') }}">
                <i class="icon-plus-sign"></i>
                Crear nuevo Estudiante Boletin
            </a>
        </li>
    </ul>
    {% endblock %}
