{% extends 'TemasTemasBundle::admin.html.twig' %}

{% block article %}

    <h1>EstudianteBoletin</h1>

    <table class="table table-striped table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <th>Id Estudiante</th>
                <th>Id Boletin</th>
                <th>Id Periodo</th>
            </tr>
            <tr>
                <td>{{ entity.id }}</td>
                <td>{{ entity.idEstudiante }}</td>
                <td>{{ entity.idBoletin }}</td>
                <td>{{ entity.idPeriodo }}</td>
            </tr>
        </tbody>
    </table>

<ul class="record_actions">
    <li>
        <a href="{{ path('estudianteboletin_edit', { 'id': entity.id }) }}" class="btn btn-small btn-success"><i class="icon-edit"> </i></a>
        {{ form(delete_form) }}
    </li>
    <li>
        <a href="{{ path('estudianteboletin') }}"><i class="icon-arrow-left"> Volver a la lista</i></a>
    </li>
</ul>
{% endblock %}
