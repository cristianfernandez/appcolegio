{% extends 'TemasTemasBundle::admin.html.twig' %}

{% block article %}
    <h1>Estrato edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('estrato') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
