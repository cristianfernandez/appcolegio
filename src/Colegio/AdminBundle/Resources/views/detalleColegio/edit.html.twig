{% extends 'TemasTemasBundle::admin.html.twig' %}

{% block article %}

<h1>Editar Detalle Colegio</h1>

{{ form(edit_form) }}
{% for rol in app.user.roles %}
        {# Solo puede borrar el Super Man  #}
        {% if rol.role == 'ROLE_SUPER_ADMIN' %}
            {{ form(delete_form) }}
        {% else %}

        {% endif %}
{% endfor %}
<a href="{{ path('detallecolegio') }}"><i class="icon-arrow-left"> Volver</i></a>

{% endblock %}
