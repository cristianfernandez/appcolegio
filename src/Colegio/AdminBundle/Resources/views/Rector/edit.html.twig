{% extends 'TemasTemasBundle::admin.html.twig' %}

{% block article %}
    <h1>Rector edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('rector') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
