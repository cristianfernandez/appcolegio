{% extends 'TemasTemasBundle::admin.html.twig' %}

{% block article %}
    <h1>Colegio edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('colegio') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
