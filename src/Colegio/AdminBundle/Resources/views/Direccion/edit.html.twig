{% extends 'TemasTemasBundle::admin.html.twig' %}

{% block article %}
    <h1>Editar Dirección</h1>

{{ form(edit_form) }}
<a href="{{ path('direccion') }}">
    Regresar
</a>
{% for rol in app.user.roles %}
    {#  Solo el Super Man Papa #}
    {% if rol.role == 'ROLE_SUPER_ADMIN' %}
        {{ form(delete_form) }}
    {% else %}
    {% endif %}
{% endfor %}
    



{% endblock %}
