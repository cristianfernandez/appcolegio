{% extends 'TemasTemasBundle::admin.html.twig' %}

{% block article %}
    <h1>TipoUsuario list</h1>

    <table class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombretipousuario</th>
                <th>Role</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tipousuario_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombreTipoUsuario }}</td>
                <td>{{ entity.role }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('tipousuario_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tipousuario_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tipousuario_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
